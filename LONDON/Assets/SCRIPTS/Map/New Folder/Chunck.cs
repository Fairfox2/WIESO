using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Chunck : MonoBehaviour
{
    /*
     #region Chuncks
    Grid_script<Grid_opjekt> grid = Map.grid;
    private void Wald_Chunck()
    {
        /*
         *  Hier spawnt ein grösser Wald mit vlt ein wenig stein   
         *

        int walt_an = Random.Range(4, 6);
        int Stein_an = 0;
        if (walt_an <= 3)                           // false der Wald zu klein ist spawnd sicher ein bis zwei Steine 
        {
            Stein_an = Random.Range(1, 3);
        }
        else                                        // false der Wald zu gross ist spawnd nur ein oder gra kein Stein
        {
            Stein_an = Random.Range(0, 2);
        }
        Rohstoffe.singleton.wals_Wiese(grid, walt_an);
        Rohstoffe.singleton.Stein_Genarator(grid, Stein_an);
    }
    private void Normal_Chunck()
    {
        /*
        *  Hier spawnt nur stein und wald 
        *

        int walt_an = Random.Range(1, 2);
        int Stein_an = Random.Range(1, 4);
        Rohstoffe.singleton.wals_Wiese(grid, walt_an);
        Rohstoffe.singleton.Stein_Genarator(grid, Stein_an);
    }
    private void Stein_Chunck()
    {
        int wald_an = 0;
        int Stein_an = Random.Range(4, 8);
        if (wald_an <= 8)                           // false der Wald zu klein ist spawnd sicher ein bis zwei Steine 
        {
            wald_an = Random.Range(1, 3);
        }
        else                                        // false der Wald zu gross ist spawnd nur ein oder gra kein Stein
        {
            wald_an = Random.Range(0, 3);
        }
        Rohstoffe.singleton.wals_Wiese(grid, wald_an);
        Rohstoffe.singleton.Stein_Genarator(grid, Stein_an);
    }
    private void Lehm_Chunck()
    {
        /*
         * 
         *
        int wald_an = 0;
        int Stein_an = 0;
        int hilf = Random.Range(1, 3);
        int Lehm_an = Random.Range(4, 7);
        if (hilf == 1)                          // false der Wald zu klein ist spawnd sicher ein bis zwei Steine 
        {
            wald_an = Random.Range(0, 1);
        }
        else                                        // false der Wald zu gross ist spawnd nur ein oder gra kein Stein
        {
            Stein_an = Random.Range(0, 3);
        }
        Rohstoffe.singleton.wals_Wiese(grid, wald_an);
        Rohstoffe.singleton.Stein_Genarator(grid, Stein_an);
        Rohstoffe.singleton.Lehm_Genarator(grid, Lehm_an);
    }
    #endregion
    */
}
